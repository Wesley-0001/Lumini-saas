{
  "name": "lumini-firestore-migrations",
  "private": true,
  "version": "1.0.0",
  "description": "One-shot Firestore migration scripts for Lumini.",
  "type": "module",
  "scripts": {
    "migrate:attendance": "node ./migrate-attendance.mjs",
    "cleanup:attendance": "node ./migrate-attendance.mjs --cleanup"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.6.0",
    "firebase-admin": "^13.5.0"
  }
}

